# GDL-Web Environment Variables
# Copy this file to .env and update the values for your environment

# Flask Environment (development, production, testing)
FLASK_ENV=development

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Security (IMPORTANT: Generate your own secure keys for production)
SECRET_KEY=your_secure_secret_key_here
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"

# Cookie Encryption (IMPORTANT: Generate your own secure key for production)
COOKIES_ENCRYPTION_KEY=your_secure_encryption_key_here
# Generate with: python -c "import base64; import os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"

# Application Configuration
DOWNLOADS_DIR=downloads
MAX_CONTENT_LENGTH=16777216  # 16MB (16 * 1024 * 1024)

# Security Configuration
COOKIES_DIR=secure_cookies

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=app.log

# Gallery-dl Configuration (optional)
# GALLERY_DL_CONFIG_PATH=/path/to/gallery-dl/config.json

# Optional: Override default paths for different environments
# For Docker/containerized deployments:
# DOWNLOADS_DIR=/app/downloads
# COOKIES_DIR=/app/secure_cookies